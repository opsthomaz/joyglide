# Development-only dependencies. Install with:
#   pip install -r requirements-dev.txt
# Used by the CI lint and test workflows; not bundled into release artifacts.

# Test runner + coverage
pytest>=9.0.3
pytest-cov>=5.0

# Static analysis (also runs in CI lint workflow)
ruff>=0.15.15
mypy>=2.1.0
pyright>=1.1.380
bandit>=1.9.4
vulture>=2.16
interrogate>=1.7
deptry>=0.25.1
radon>=6.0.1
xenon>=0.9.3
codespell>=2.4.2
yamllint>=1.38.0
import-linter>=2.0
hypothesis>=6.155.1
pip-audit>=2.7
mutmut>=3.5.0
# cosmic-ray covers the parser/ package where mutmut hits its package-import
# wall — see packaging/run_cosmic_ray.sh for the workflow. Validated 100%
# mutation score on parser/u16_delta.py.
cosmic-ray>=8.4
cohesion>=1.2

# Pre-commit hooks (optional — `pre-commit install` to enable)
pre-commit>=4.6.0
